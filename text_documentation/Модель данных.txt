#Заявка (Ticket)
# id
# тип сценария (1–6)
# отдел
# зона (nullable)
# тип проблемы
# приоритет
# статус
# филиал
# автор
# текущий исполнитель (отдел)
# комментарий
# история статусов
# дата создания
# дата закрытия
# SLA дедлайн реакции
# SLA дедлайн выполнения
# 📌 Отдельно:
# причина отклонения
# отметка “частично выполнено” (да / нет)
#
# id: Автоматически выдается порядковый номер заявки.
# Тип сценария(крит уведомление, крит заявка, срочная заявка, обычная заявка, поставка, уведомление, инцидент)
# Отдел(рук, АРС, IT, склад, инвентарь, маркетинг, вызывная служба, HR, бухгалтер, СБ, сервис)
# Зона(бар, касса, торговый зал, зона продаж, зона выпечки, стафф, уборная, зона техперсонала, внешняя территория)
# Тип проблемы(перечень зависит от зоны) # тут или в справочник нужно перечислять?
# Приоритет(крит, высокий, средний, низкий)
# Статус(создана без подтверждения, создана, в работе, ожидание, выполнено, ожидает подтверждения, отклонена, закрыта)
# Филиал(Эта сущность заранее неизвестна. её нужно создавать или при интеграции бота или дать возможность руководителю её создать и прицеплять к упрам/пользователям)
# Автор(id отправителя, зацеплять при создании из тг)
# комментарий. Практически везде стоит предлагать добавить коммент
# история статусов. Сохранять со временем. близкая тема к логированию.
# дата создания и дата закрытия считаю излишним. если сохранять статусы с временем
# SLA дедлайн реакции и SLA дедлайн выполнения сложно проставить сразу. имею примерное представление
# причина отклонения - обязателен коммент и отправка при этом сообщения руководителю
# отметка “частично выполнено” (да / нет) -
#
#
#
#
#
#
#
#